version: '3.8'
services:
  app:
    build: .
    env_file:
      - .env.example
    volumes:
      - ./:/workspace
    working_dir: /workspace
    command: ["python3", "scripts/hyde_experiment.py", "--dry-run"]
    restart: 'no'
    deploy:
      resources:
        limits:
          memory: 4G
